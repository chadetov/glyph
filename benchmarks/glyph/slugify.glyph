module slugify

import std/string
import std/regex

pub fn slugify(s: string) -> string {
  let lowered = string.lower(s)
  let dashed = regex.replace_all("[^a-z0-9]+", lowered, "-")
  return regex.replace_all("^-+|-+$", dashed, "")
}
