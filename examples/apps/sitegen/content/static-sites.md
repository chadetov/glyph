---
title: Why static sites
date: "2026-02-03"
tags:
  - web
---

A static site is a directory of files. There is no database and no server
process to keep alive, so the only thing that can break is the build.

```js
console.log("no runtime");
```
