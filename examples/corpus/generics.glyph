// Generic functions and explicit type arguments at the call site.
module generics

fn identity<T>(x: T) -> T {
  return x
}

fn first<T>(a: T, b: T) -> T {
  return a
}

fn demo() -> number {
  return first<number>(identity<number>(1), 2)
}
