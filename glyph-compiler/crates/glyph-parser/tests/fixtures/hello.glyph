module hello

import std/io
import std/result { Result, Ok, Err }
import std/http as h

fn add(a: number, b: number) -> number {
  return a + b
}

async fn greet(name: string) -> Result<number, string> {
  let message = "hello"
  io.println(message)
  let x = 1 + 2 * 3
  let y = arr[0]
  let z = foo()?
  return Ok(0)
}
