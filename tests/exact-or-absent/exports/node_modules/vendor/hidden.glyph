module hidden

// Under `node_modules`, which the project walk does not enter, so this file is
// not a member of the project and `hidden` is a module it does not hold.
pub fn ignore() -> number {
  return 1
}
